#include<stdio.h>

main()
{
    int i;  // 'int' is a datatype of the variable, which is used to know the type of the variable. Other datatypes are char, float etc.
    int j;
    i=2;
    j=3;
    int k=i+j;

    printf("The result is %d",k);
}
