#include<stdio.h>

main()
{
    int i;  // 'int' is a datatype of the variable, which is used to know the type of the variable. It tskes 16 bits or 2 bytes of memory. Other datatypes are char, float etc.
    int j;
    i=2;
    j=3;
    int k=i+j;

    //printf("%d",k);
    //printf("The result is %d",k);
    printf("The addition result of %d & %d is %d",i,j,k);
}
