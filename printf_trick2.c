/* A 'printf()' function trick. */
/* Printing without semicolon (i.e. ';'). */


#include<stdio.h>
 int main()
 {
     if(printf("hello\n"))  // This 'printf()' method returns something. And 'if' needs that data.
     {

     }
 }
